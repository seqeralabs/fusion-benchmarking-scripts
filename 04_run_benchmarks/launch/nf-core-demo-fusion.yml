launch:
  - name: "nf-core-demo-fusion-$TIME"
    workspace: "$ORGANIZATION_NAME/$WORKSPACE_NAME"
    compute-env: "${COMPUTE_ENV_PREFIX}_fusion_nvme"
    pipeline: "nf-core-demo"
    params:
      outdir: "${PIPELINE_OUTDIR_PREFIX}/nf-core-demo/fusion-results"
